mainmodule hello {
  readonly CProxy_Main mainProxy;

  mainchare Main {
    entry Main(CkArgMsg *m);
    entry void done(double result);
  };

  chare Hello {
    entry Hello(int id, int p);
    entry void SayHi(int hiNo);
  };        	
};
